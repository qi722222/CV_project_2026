# DiffuEraser GT mask v4（hard blend，消除软融合泄漏）

## 这是什么

基于 v1 inpaint_out 做后处理硬贴回：mask 内保留 DiffuEraser 修复结果，mask 外强制使用 DAVIS 原始 JPEG 帧（不经 MP4 重编码）。目的：消除 soft-blending 泄漏导致的白色虚影问题。此版不重跑 DiffuEraser 推理，是对 v1 的纯后处理。

## 这条实验属于哪一类

- `inpaint_only`
- 家族：DiffuEraser
- 审计状态：`exploratory`
- 版本：`v4`
- mask 协议：`davis_gt`
- 对比基线：`diffueraser_gtmask_v1`

## 固定四问

### Q1  这条实验到底在比什么？
基于 v1 inpaint_out 做后处理硬贴回：mask 内保留 DiffuEraser 修复结果，mask 外强制使用 DAVIS 原始 JPEG 帧（不经 MP4 重编码）。目的：消除 soft-blending 泄漏导致的白色虚影问题。此版不重跑 DiffuEraser 推理，是对 v1 的纯后处理。

### Q2  这个实验用的 mask 是哪来的？
- **mask_protocol**: `davis_gt`
- DAVIS 默认：`DAVIS annotation / GT mask`，路径 `/home/jli657/shared_data/project3/DAVIS/Annotations/480p/<seq>`
- 非 DAVIS 或 wild video 必须在 `mask_protocol` 字段中说明来源。

### Q3  修复视频是哪个工具做的？
- 修复工具见 `family`：`DiffuEraser`
- 典型工具：ProPainter（PP）、LaMa、SDXL、DiffuEraser、VOID、ControlNet

### Q4  结果不好时，是 mask 的锅，还是 inpainting 的锅？
- 先看 `masked_in.mp4`：覆盖目标是否完整？有无误伤背景？时间是否抖动？
- 再看 `inpaint_out.mp4`：修复区域是否自然？是否闪烁 / hallucination？
- 只有两个视频都合理，`PSNR / SSIM / JM / JR / F` 指标才能作为结论证据。

## 你看这个实验时重点看什么

1. inpaint_out.mp4：非 mask 区域是否完全等同于原始视频，虚影是否消除
2. PSNR_proxy：应显著提升（理论上 mask 外像素完全一致）
3. PSNR_synthetic：应与 v1 相近（mask 内像素未变）
4. 视觉质量：mask 边界是否有硬切割感

## 当前结论

v4 结果（硬贴回，从 DAVIS JPEG 直接取非 mask 像素）：PSNR_proxy=36.28（+1.40dB vs 基线 34.88），通过 PSNR_proxy 门槛！PSNR_synthetic=32.88（mask 区域修复质量高，与 ProPainter prior 一致）。白色虚影问题已通过硬贴回解决。决策：v4 通过门槛，可扩到其他序列。

## Version History / 版本演化

| 字段 | 值 |
|---|---|
| version | `v4` |
| based_on | `diffueraser_gtmask_v1` |
| changes_from_previous | 见下方"当前结论"与 `exp_id` 命名增量 |
| motivation | 1. inpaint_out.mp4：非 mask 区域是否完全等同于原始视频，虚影是否消除
2. PSNR_proxy：应显著提升（理论上 mask 外像素完全一致）
3. PSNR_synthetic：应与 v1 相近（mask 内像素未变）
4. 视觉质量：mask 边界是否有硬切割感 |
| expected_gain | 见 `plain_explanation` |
| actual_result | v4 结果（硬贴回，从 DAVIS JPEG 直接取非 mask 像素）：PSNR_proxy=36.28（+1.40dB vs 基线 34.88），通过 PSNR_proxy 门槛！PSNR_synthetic=32.88（mask 区域修复质量高，与 ProPainter prior 一致）。白色虚影问题已通过硬贴回解决。决策：v4 通过门槛，可扩到其他序列。 |
| next_decision | v4 通过 PSNR_proxy 门槛（36.28 ≥ 34.88）。下一步：扩到 bmx-trees 和 car-shadow 验证泛化性。 |
- **stage_gate**: PSNR_proxy>=34.88 AND SSIM>=0.92

## 关键路径

- `inpaint_out`: `/data3/jli657/project3/part3/results/tennis/direction_c/diffueraser_gtmask_v4/inpaint_out.mp4`
- `input_video`: `/data3/jli657/project3/part3/results/tennis/direction_c/diffueraser_gtmask_v4/input_video.mp4`
- `input_mask`: `/data3/jli657/project3/part3/results/tennis/direction_c/diffueraser_gtmask_v4/input_mask.mp4`
- `masked_in`: `/data3/jli657/project3/part3/results/tennis/direction_c/diffueraser_gtmask_v4/masked_in.mp4`
- `mask_frames`: `/data3/jli657/project3/part3/results/tennis/direction_c/diffueraser_gtmask_v4/mask_frames`
- `manifest`: `/data3/jli657/project3/part3/results/tennis/direction_c/diffueraser_gtmask_v4/run_manifest.json`
- `script_path`: `/data3/jli657/project3/part3/inpainting/apply_hard_blend.py`

## 指标摘录

- 当前未在统一指标表里找到对应数值。

---
*generated by build_part3_deliverables.py — schema v2*
