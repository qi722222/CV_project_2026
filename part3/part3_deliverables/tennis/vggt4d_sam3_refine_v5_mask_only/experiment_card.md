# VGGT4D + SAM3 refine v5

## 这是什么

这是当前 Direction B 最终保留的 refine 版本：先自动发现动态区域，再用 SAM3 把边缘精修。

## 这条实验属于哪一类

- `mask_only`
- 家族：Direction B（Better Mask）
- 审计状态：`stable`
- 版本：`legacy`
- mask 协议：`未指定`
- 对比基线：`无`

## 固定四问

### Q1  这条实验到底在比什么？
这是当前 Direction B 最终保留的 refine 版本：先自动发现动态区域，再用 SAM3 把边缘精修。

### Q2  这个实验用的 mask 是哪来的？
- **mask_protocol**: `未显式指定，请补充 mask_protocol 字段`
- DAVIS 默认：`DAVIS annotation / GT mask`，路径 `/home/jli657/shared_data/project3/DAVIS/Annotations/480p/<seq>`
- 非 DAVIS 或 wild video 必须在 `mask_protocol` 字段中说明来源。

### Q3  修复视频是哪个工具做的？
- 修复工具见 `family`：`direction_b`
- 典型工具：ProPainter（PP）、LaMa、SDXL、DiffuEraser、VOID、ControlNet

### Q4  结果不好时，是 mask 的锅，还是 inpainting 的锅？
- 先看 `masked_in.mp4`：覆盖目标是否完整？有无误伤背景？时间是否抖动？
- 再看 `inpaint_out.mp4`：修复区域是否自然？是否闪烁 / hallucination？
- 只有两个视频都合理，`PSNR / SSIM / JM / JR / F` 指标才能作为结论证据。

## 你看这个实验时重点看什么

重点看它是否成功把粗糙的无监督发现，变成了真正可用的高质量 mask。

## 当前结论

这是 Direction B 当前最有说服力的正向结果。

## Version History / 版本演化

| 字段 | 值 |
|---|---|
| version | `legacy` |
| based_on | `（首版或无可比基线）` |
| changes_from_previous | 见下方"当前结论"与 `exp_id` 命名增量 |
| motivation | 重点看它是否成功把粗糙的无监督发现，变成了真正可用的高质量 mask。 |
| expected_gain | 见 `plain_explanation` |
| actual_result | 这是 Direction B 当前最有说服力的正向结果。 |
| next_decision | 待实验结果更新 |

## 关键路径

- `mask_frames`: `/data3/jli657/project3/part3/outputs/direction_b/sam3_refined_v5/vggt4d/tennis`
- `rough_mask_frames`: `/data3/jli657/project3/part3/outputs/direction_b/vggt4d_vggt/tennis`
- `script_path`: `/data3/jli657/project3/part3/direction_b/run_direction_b_sam3_refine.py`

## 指标摘录

- `status`: `success`
- `source`: `vggt4d`
- `prompt_mode`: `bbox`
- `prompt_frame`: `00000`
- `JM`: `0.8735063675684897`
- `JR`: `1.0`
- `F`: `0.3420117754446857`
- `rough_JM`: `0.7571360899399955`

---
*generated by build_part3_deliverables.py — schema v2*
