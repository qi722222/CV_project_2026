# GDINO + SAM3 + RealVLM prompt

## 这是什么

这条路线用真实 VLM caption 来生成 prompt，而不是完全手写规则。

## 这条实验属于哪一类

- `mask_only`
- 家族：Direction A（Mask Upgrade）
- 审计状态：`exploratory`
- 版本：`legacy`
- mask 协议：`未指定`
- 对比基线：`无`

## 固定四问

### Q1  这条实验到底在比什么？
这条路线用真实 VLM caption 来生成 prompt，而不是完全手写规则。

### Q2  这个实验用的 mask 是哪来的？
- **mask_protocol**: `未显式指定，请补充 mask_protocol 字段`
- DAVIS 默认：`DAVIS annotation / GT mask`，路径 `/home/jli657/shared_data/project3/DAVIS/Annotations/480p/<seq>`
- 非 DAVIS 或 wild video 必须在 `mask_protocol` 字段中说明来源。

### Q3  修复视频是哪个工具做的？
- 修复工具见 `family`：`direction_a`
- 典型工具：ProPainter（PP）、LaMa、SDXL、DiffuEraser、VOID、ControlNet

### Q4  结果不好时，是 mask 的锅，还是 inpainting 的锅？
- 先看 `masked_in.mp4`：覆盖目标是否完整？有无误伤背景？时间是否抖动？
- 再看 `inpaint_out.mp4`：修复区域是否自然？是否闪烁 / hallucination？
- 只有两个视频都合理，`PSNR / SSIM / JM / JR / F` 指标才能作为结论证据。

## 你看这个实验时重点看什么

看它是不是只是在‘说法不同但效果相同’，还是确实更懂图像内容。

## 当前结论

当前已验证能跑通，但已测序列上没有额外数值收益。

## Version History / 版本演化

| 字段 | 值 |
|---|---|
| version | `legacy` |
| based_on | `（首版或无可比基线）` |
| changes_from_previous | 见下方"当前结论"与 `exp_id` 命名增量 |
| motivation | 看它是不是只是在‘说法不同但效果相同’，还是确实更懂图像内容。 |
| expected_gain | 见 `plain_explanation` |
| actual_result | 当前已验证能跑通，但已测序列上没有额外数值收益。 |
| next_decision | 待实验结果更新 |

## 关键路径

- `mask_frames`: `/data3/jli657/project3/part3/gdino_vlm/masks/sam3/innovation/real_vlm/bmx-trees`
- `run_meta`: `/data3/jli657/project3/part3/gdino_vlm/masks/sam3/innovation/real_vlm/bmx-trees/run_meta.json`
- `script_path`: `/data3/jli657/project3/part3/gdino_vlm/run_gdino_mainline.py`
- `config_path`: `/data3/jli657/project3/part3/configs/gdino_vlm_sam3_innov_real_vlm.yaml`

## 指标摘录

- `JM`: `0.365081`
- `JR`: `0.3375`
- `F`: `0.453436`

---
*generated by build_part3_deliverables.py — schema v2*
