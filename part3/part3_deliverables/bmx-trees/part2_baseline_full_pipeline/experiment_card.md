# Part2 YOLO+SAM2+ProPainter 基线完整流程

## 这是什么

这是旧基线：YOLO+SAM2 先出 mask，再用 ProPainter 修复。

## 这条实验属于哪一类

- `full_pipeline`
- 家族：基线/历史参照
- 审计状态：`reference`
- 版本：`legacy`
- mask 协议：`未指定`
- 对比基线：`无`

## 固定四问

### Q1  这条实验到底在比什么？
这是旧基线：YOLO+SAM2 先出 mask，再用 ProPainter 修复。

### Q2  这个实验用的 mask 是哪来的？
- **mask_protocol**: `未显式指定，请补充 mask_protocol 字段`
- DAVIS 默认：`DAVIS annotation / GT mask`，路径 `/home/jli657/shared_data/project3/DAVIS/Annotations/480p/<seq>`
- 非 DAVIS 或 wild video 必须在 `mask_protocol` 字段中说明来源。

### Q3  修复视频是哪个工具做的？
- 修复工具见 `family`：`baseline`
- 典型工具：ProPainter（PP）、LaMa、SDXL、DiffuEraser、VOID、ControlNet

### Q4  结果不好时，是 mask 的锅，还是 inpainting 的锅？
- 先看 `masked_in.mp4`：覆盖目标是否完整？有无误伤背景？时间是否抖动？
- 再看 `inpaint_out.mp4`：修复区域是否自然？是否闪烁 / hallucination？
- 只有两个视频都合理，`PSNR / SSIM / JM / JR / F` 指标才能作为结论证据。

## 你看这个实验时重点看什么

先看 masked_in.mp4 判断旧 mask 是否稳，再看 inpaint_out.mp4 作为全流程参照。

## 当前结论

它不是最先进，但它很重要，因为很多新方法必须先超过它才有说服力。

## Version History / 版本演化

| 字段 | 值 |
|---|---|
| version | `legacy` |
| based_on | `（首版或无可比基线）` |
| changes_from_previous | 见下方"当前结论"与 `exp_id` 命名增量 |
| motivation | 先看 masked_in.mp4 判断旧 mask 是否稳，再看 inpaint_out.mp4 作为全流程参照。 |
| expected_gain | 见 `plain_explanation` |
| actual_result | 它不是最先进，但它很重要，因为很多新方法必须先超过它才有说服力。 |
| next_decision | 待实验结果更新 |

## 关键路径

- `mask_frames`: `/data3/jli657/project3/part2/masks_cache/bmx-trees_bbox_union`
- `source_output_dir`: `/data3/jli657/project3/part2/outputs/bmx-trees_v2/bmx-trees`
- `inpaint_out`: `/data3/jli657/project3/part2/outputs/bmx-trees_v2/bmx-trees/inpaint_out.mp4`
- `masked_in`: `/data3/jli657/project3/part2/outputs/bmx-trees_v2/bmx-trees/masked_in.mp4`

## 指标摘录

- 当前未在统一指标表里找到对应数值。

---
*generated by build_part3_deliverables.py — schema v2*
