# ControlNet 消融：hybrid + temporal consistency

## 这是什么

这是在 hybrid 的基础上，再加一层更保守的时序一致性处理。

## 这条实验属于哪一类

- `inpaint_only`
- 家族：Direction C（Inpainting）
- 审计状态：`exploratory`
- 版本：`legacy`
- mask 协议：`未指定`
- 对比基线：`无`

## 固定四问

### Q1  这条实验到底在比什么？
这是在 hybrid 的基础上，再加一层更保守的时序一致性处理。

### Q2  这个实验用的 mask 是哪来的？
- **mask_protocol**: `未显式指定，请补充 mask_protocol 字段`
- DAVIS 默认：`DAVIS annotation / GT mask`，路径 `/home/jli657/shared_data/project3/DAVIS/Annotations/480p/<seq>`
- 非 DAVIS 或 wild video 必须在 `mask_protocol` 字段中说明来源。

### Q3  修复视频是哪个工具做的？
- 修复工具见 `family`：`direction_c`
- 典型工具：ProPainter（PP）、LaMa、SDXL、DiffuEraser、VOID、ControlNet

### Q4  结果不好时，是 mask 的锅，还是 inpainting 的锅？
- 先看 `masked_in.mp4`：覆盖目标是否完整？有无误伤背景？时间是否抖动？
- 再看 `inpaint_out.mp4`：修复区域是否自然？是否闪烁 / hallucination？
- 只有两个视频都合理，`PSNR / SSIM / JM / JR / F` 指标才能作为结论证据。

## 你看这个实验时重点看什么

重点看它是否真减少闪烁，还是只是看起来更稳但细节没有更好。

## 当前结论

这是个合理尝试，但目前仍然很难整体超过纯 ProPainter。

## Version History / 版本演化

| 字段 | 值 |
|---|---|
| version | `legacy` |
| based_on | `（首版或无可比基线）` |
| changes_from_previous | 见下方"当前结论"与 `exp_id` 命名增量 |
| motivation | 重点看它是否真减少闪烁，还是只是看起来更稳但细节没有更好。 |
| expected_gain | 见 `plain_explanation` |
| actual_result | 这是个合理尝试，但目前仍然很难整体超过纯 ProPainter。 |
| next_decision | 待实验结果更新 |

## 关键路径

- `repair_frames`: `/data3/jli657/project3/part3/outputs/controlnet/ablation_5seq/bmx-trees/hybrid_tc_frames`
- `source_output_dir`: `/data3/jli657/project3/part3/outputs/controlnet/ablation_5seq/bmx-trees/propainter_hybrid_tc/hybrid_tc_frames`
- `inpaint_out`: `/data3/jli657/project3/part3/outputs/controlnet/ablation_5seq/bmx-trees/propainter_hybrid_tc/hybrid_tc_frames/inpaint_out.mp4`
- `masked_in`: `/data3/jli657/project3/part3/outputs/controlnet/ablation_5seq/bmx-trees/propainter_hybrid_tc/hybrid_tc_frames/masked_in.mp4`
- `script_path`: `/data3/jli657/project3/part3/inpainting/run_controlnet_ablation_5seq.py`

## 指标摘录

- `JM`: `1.0`
- `JR`: `1.0`
- `PSNR_proxy`: `35.00235152219808`
- `PSNR_synthetic`: `31.938589350478786`
- `SSIM_proxy`: `0.9974640491403477`
- `SSIM_synthetic`: `0.9947610714807776`
- `recommended_method`: `pure_propainter`

---
*generated by build_part3_deliverables.py — schema v2*
